# hardhat-upgradeable-token

Hardhat + OpenZeppelin **UUPS** upgradeable ERC20 (V1 mintable, V2 adds pause). Demonstrates proxy upgrades for EVM portfolios.

```bash
npm install
npx hardhat compile
npx hardhat test
```

See [`docs/pull-requests/`](./docs/pull-requests/).

Not audited.
